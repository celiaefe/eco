# Data notes

- `data/backup/recuerdos_legacy.json` is legacy data kept only as backup/migration source.
- Runtime now reads/writes memories from database table `memory`.
- `data/recuerdos.json` is no longer part of the active application flow.
